import os

# select the directory whose content you want to list

directory_path = '/'

# use the os mpodule to list the directory content
constent = os.listdir(directory_path)

# print the content of the directory 
print(directory_path)
